{
  "version": 2,
  "builds": [
    {
      "src": "Gesture/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "Gesture/dist"
      }
    }
  ],
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}